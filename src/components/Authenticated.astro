---
import { getApi } from "@/bknd.ts";

const api = await getApi(Astro.request.headers, { verify: true, mode: "dynamic" });
const user = api.getUser();
---

{user?.email ? <slot /> : <slot name="fallback" />}
